# Even or Odd
Create a function (or write a script in Shell) that takes an integer as an argument and returns "Even" for even numbers or "Odd" for odd numbers.

# Google翻訳
整数を引数として取り、偶数の場合は「偶数」、奇数の場合は「奇数」を返す関数を作成します
(またはシェルでスクリプトを記述します）。
